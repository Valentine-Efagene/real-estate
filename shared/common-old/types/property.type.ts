export enum PropertyCategory {
    RENT = 'RENT',
    SALE = 'SALE',
}

export enum PropertyStatus {
    APPROVED = 'APPROVED',
    PENDING = 'PENDING',
    DECLINED = 'DECLINED',
}

export enum PropertyType {
    APARTMENT = 'APARTMENT',
    HOUSE = 'HOUSE',
}