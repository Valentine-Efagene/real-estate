<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{title}}</title>
  <link href="https://fonts.cdnfonts.com/css/satoshi" rel="stylesheet" />
  <style>
    body {
      margin: 0;
      padding: 0;
      background-color: #f0f0f0;
      font-family: 'Satoshi', sans-serif;
    }

    table {
      border-spacing: 0;
      width: 100%;
      max-width: 600px;
      margin: 0 auto;
      background: #ffffff;
      border-radius: 10px;
    }

    a {
      color: inherit;
      text-decoration: none;
    }

    img {
      display: block;
      border: 0;
      max-width: 100%;
    }

    ul {
      padding-left: 20px;
    }

    li {
      margin-bottom: 0.5rem;
    }

    .footer-text {
      font-size: 10px;
      font-weight: 400;
      color: #ffffff;
      text-align: center;
      margin: 0.2rem 0;
    }
  </style>
</head>
<body>
  <div style="background: #f0f0f0; padding: 1rem 0;">
    <table>
      <!-- Header -->
      {{{ header }}}

      <!-- Dynamic Body -->
      <tr>
        <td style="padding: 1rem 5%;">
          {{{body}}}
        </td>
      </tr>

      <!-- Footer -->
      {{{ footer }}}
    </table>
  </div>
</body>
</html>
