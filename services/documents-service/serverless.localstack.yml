# LocalStack-specific serverless config for Documents Service
# Uses REST API (v1) for better LocalStack compatibility
# Usage: npx sls deploy --config serverless.localstack.yml --stage localstack
# Requires: LocalStack running with CDK deployed (cd infrastructure && pnpm localstack:deploy)

service: qshelter-documents-service

frameworkVersion: '4'

provider:
  name: aws
  runtime: nodejs20.x
  region: us-east-1
  stage: ${opt:stage, 'localstack'}
  memorySize: 512
  timeout: 30
  
  # REST API (v1) for LocalStack compatibility - no authorizer
  apiGateway:
    shouldStartNameWithService: true

  environment:
    NODE_ENV: ${self:provider.stage}
    STAGE: ${self:provider.stage}
    LOCALSTACK_ENDPOINT: http://host.docker.internal:4566
    # Database credentials from SSM
    DB_HOST: ${ssm:/qshelter/${self:provider.stage}/DB_HOST}
    DB_PORT: ${ssm:/qshelter/${self:provider.stage}/DB_PORT}
    DB_USER: ${ssm:/qshelter/${self:provider.stage}/DB_USER}
    DB_PASSWORD: ${ssm:/qshelter/${self:provider.stage}/DB_PASSWORD}
    DB_NAME: ${ssm:/qshelter/${self:provider.stage}/DB_NAME}

  iam:
    role:
      statements:
        # SSM Parameter Store access for infrastructure config
        - Effect: Allow
          Action:
            - ssm:GetParameter
            - ssm:GetParameters
            - ssm:GetParametersByPath
          Resource:
            - arn:aws:ssm:${self:provider.region}:*:parameter/qshelter/${self:provider.stage}/*
        # Secrets Manager access for sensitive credentials
        - Effect: Allow
          Action:
            - secretsmanager:GetSecretValue
          Resource:
            - arn:aws:secretsmanager:${self:provider.region}:*:secret:qshelter/${self:provider.stage}/*
        # EventBridge access
        - Effect: Allow
          Action:
            - events:PutEvents
          Resource:
            - arn:aws:events:${self:provider.region}:*:event-bus/*
        # SNS access for notifications
        - Effect: Allow
          Action:
            - sns:Publish
          Resource:
            - arn:aws:sns:${self:provider.region}:*:*

package:
  individually: true
  patterns:
    # Exclude everything first
    - '!**'
    # Include only the bundled Lambda entry
    - 'dist/**/*.mjs'
    # Prisma client runtime dependencies
    - 'node_modules/.prisma/**'
    - 'node_modules/@prisma/client/**'
    - 'node_modules/@prisma/adapter-mariadb/**'
    - 'node_modules/@prisma/client-runtime-utils/**'
    - 'node_modules/@prisma/driver-adapter-utils/**'
    - 'node_modules/@prisma/debug/**'
    - 'node_modules/mariadb/**'
    # Exclude unused Prisma runtime adapters
    - '!node_modules/@prisma/client/runtime/*cockroachdb*'
    - '!node_modules/@prisma/client/runtime/*sqlserver*'
    - '!node_modules/@prisma/client/runtime/*postgresql*'
    - '!node_modules/@prisma/client/runtime/*sqlite*'

functions:
  main:
    handler: dist/lambda.handler
    events:
      # REST API (v1) events - no authorizer for LocalStack simplicity
      - http:
          path: /{proxy+}
          method: ANY
          cors: true
      - http:
          path: /health
          method: GET
          cors: true
      - http:
          path: /api-docs
          method: GET
          cors: true
      - http:
          path: /openapi.json
          method: GET
          cors: true

plugins:
  - serverless-localstack

custom:
  localstack:
    stages:
      - localstack
    host: http://localhost
    edgePort: 4566
    autostart: false
