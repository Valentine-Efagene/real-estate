## Scenario 1

- Assume a property exists (seeded)
- Admin (seeded) creates a payment method that
  - has a documentation phase where the user uploads credit score, bank statement.
  - approval gate
  - has a payment phase where the user pays 10% of the property value one-off
  - If the
