# LocalStack development environment
NODE_ENV=localstack
STAGE=localstack

# Database (Docker MySQL on port 3307)
DB_HOST=127.0.0.1
DB_PORT=3307
DB_USER=root
DB_PASSWORD=rootpassword
DB_NAME=qshelter_test

# LocalStack endpoint for AWS SDK clients (SSM, Secrets Manager, S3, SNS)
LOCALSTACK_ENDPOINT=http://localhost:4566

# Service port
PORT=3001
